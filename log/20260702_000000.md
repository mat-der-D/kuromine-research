# Session Log

**Session ID:** 20260702_000000 (Session 33)
**Date:** 2026-06-26 (scheduled run; next-timestamp convention 20260702_000000, after the latest
existing log 20260701_000000)
**Status:** WATCH-phase arXiv watch (**negative, twenty-second confirmation**) followed by a
concrete Wolfram computation producing a **genuinely new, verified structural result**: the
**2-adic Newton/tangent expansion** of the bijection $k_y\mapsto A$ (Theorem R), which sharpens
the Session-32 Theorem P from a period statement to an explicit 2-adic analytic Mahler expansion,
and completes the 2-adic/3-adic tangent-valuation symmetry. Like every prior structural finding it
**confirms (does not breach)** the two-comparable-S-units wall. **No theory-building** (per the
Session-28 inventory-complete verdict, re-affirmed S29–S32).

*Wolfram MCP available for most of the session (frame constants + the core 2-adic computations);
the endpoint returned intermittent 502 gateway errors near the end, leaving one auxiliary
higher-depth confirmation pending — noted honestly below.*

---

## Direction Chosen

**[WATCH — the sole active priority]** plus a focused Wolfram experiment on the one remaining
unexamined facet of the now-complete $p$-adic portrait of $A=z-3^j$: the **fine analytic
structure** of the 2-adic bijection $k_y\mapsto A$ established in Session 32 (Theorem P). Theorem P
recorded that for $n\ge5$, $A\bmod2^n$ is determined by $k_y\bmod2^{n-4}$ with period exactly
$2^{n-4}$ (a bijection $\mathbb Z_2\to11+16\mathbb Z_2$), but never computed *how* $A$ varies with
$k_y$ — whether the map is affine, polynomial, or genuinely analytic, and what its tangent
valuation is. The 3-adic side has this (Session 24: tangent speed $\tau$ with $v_3(\tau)=3$); the
2-adic analogue was missing.

**Reasoning for not theory-building.** The Session-28 verdict (re-affirmed S29–S32) is explicit:
the attack-face inventory is **complete** (metric S15; analytic/irrationality S3/10/11;
3-adic-coupling S20–21/S24; decidability/degree S22; archimedean-equidistribution S27–28), and any
fourth framework collapses onto an existing face unless it beats the Dirichlet floor on the
$2^{2/3}$ approximation. Nothing in this session's watch supplies such a tool. This session
therefore (i) ran the mandated watch, (ii) executed a concrete recordable Wolfram computation —
exactly the pattern of S29 (Eisenstein-$B$ density), S31 ($E$-range), S32 (Theorem P) — and (iii)
re-anchored the frame constants. The honest expectation (borne out) was that it would confirm the
wall while filling a real gap in the documented structure.

---

## Work Done

### A. arXiv / web literature watch (negative; twenty-second confirmation)

WebSearch over: (1) effective power-saving lower bounds for $|z^d-S\text{-unit}|$ (2026, Bugeaud);
(2) decidability of linear-exponential Diophantine systems over $\ge3$ primes / Skolem (2026); (3)
effective irrationality measure / cube-root separation for $2^{1/3}$ (2026); (4) a broad
"$2^x+3^y$ perfect cube finiteness" sweep.

**All frontiers re-confirmed unchanged:**
- **Power-saving gap (Bugeaud).** arXiv:2503.22084 ("On the difference between squares and integral
  $S$-units") and the degree-$d$ generalization 2604.27490: effective lower bounds for
  $|z^d-q_1^{a_1}\cdots q_t^{a_t}|$ and its greatest prime factor that only **"tend to infinity
  with $z^d$"** — trivially met by the Kuromine gap $|z^3-3^y|=2^x+5\to\infty$; **NOT** a
  power-saving $\kappa>1$. Unchanged.
- **Decidability ($k\ge3$ primes).** Dong–Shafrir (arXiv:2505.19141, STOC 2026, "S-unit equations
  in modules and linear-exponential Diophantine equations"): solvability over $T$ with $\le2$ prime
  divisors is decidable (via Karimov–Luca–Nieuwveld–Ouaknine–Worrell 2025); the $k\ge3$-prime case
  is still **"an outstanding open problem in number theory."** Degree-3 free terms remain blocked
  (S30 arXiv:2510.00759: degree 3 is the universal-undecidability threshold). No STOC/LICS 2026
  follow-up reaches the Kuromine regime.
- **Irrationality measures.** CDT holonomy arXiv:2510.04156 and Badziahin keep $\mu(2^{1/3})>2\gg
  1.107$; and the target is sub-Dirichlet, so measures categorically cannot reach it. No 2026
  follow-up below $\sim1.1$.
- Broad "$2^x+3^y=$ cube" sweep: no new applicable paper.

**Watch verdict: negative (twenty-second confirmation of the two-comparable-S-units / degree-3 wall).**

### B. Wolfram re-anchor of frame constants (30 digits)

- $3-3\log_3 2 = 1.107210739285627688701418656972\ldots$
- $3\log2/\log3 = 1.892789260714372311298581343028\ldots$
- $2\log3/(3\log2) = 1.056641667147437454302492629299\ldots$

### C. New Wolfram computation — the fine 2-adic structure of $k_y\mapsto A$ (and $z$)

Setup (Theorem-5 family): $x=5+332640\,k_x$, $y=166317+166320\,k_y$, $j=y/3=55439+55440\,k_y$,
$z=2u$ ($u$ odd, $v_2(z)=1$ since $v_2(3^y+5)=3$ universally), $A=z-3^j$. The 2-adic cube root $u$
of $(3^y+5)/8$ is computed mod $2^n$; $A=2u-3^j\bmod2^n$.

**(C1) $A(k_y)$ and $z(k_y)\bmod2^n$ are affine (arithmetic progressions) to leading order
(Wolfram).** At $n=8$ (mod 256):
- $A\bmod256$, $k_y=0..9$: $251,43,91,139,187,235,27,75,123,171$ — exactly Theorem P's table; an
  arithmetic progression with **constant step $48$** ($v_2=4$).
- $z\bmod256$, $k_y=0..20$: $102,86,70,54,38,22,6,246,\ldots$ — constant step $-16\equiv240$
  ($v_2=4$): $z\equiv102-16\,k_y\pmod{256}$.
Both are bijective in $k_y\bmod2^{n-4}$ (distinct-value count $2^{n-4}$), confirming Theorem P, and
both have step of 2-adic valuation exactly $4$.

**(C2) Full-period linearity is exact only up to depth $n=8$; a quadratic 2-adic term enters at
$n=9$ (Wolfram).** For $n=6,7,8$ the affine form $A(k_y)\equiv A_0+s\,k_y$ holds over the full
period; for $n=9,10$ it fails — the map is a genuine 2-adic analytic bijection, not an affine one.

**(C3) Mahler/Newton expansion — the precise statement (Wolfram).** Writing
$A(k_y)=c_0+c_1k_y+c_2\binom{k_y}{2}+c_3\binom{k_y}{3}+\cdots$ with $c_k=(\Delta^kA)(0)$:
- At $n=12$ (mod 4096): first differences all have $v_2=4$; second differences all have $v_2=8$.
- At $n=16$ (mod 65536): $v_2(c_1)=4$, $v_2(c_2)=8$, $v_2(c_3)=11$ — the $c_3$ digit is crowded by
  the modulus (the expansion saturates the available depth), so it under-reads.
- At $n=24$ (mod $2^{24}$): $v_2(c_1)=4$, $v_2(c_2)=8$ confirmed cleanly.

The pattern is the **leading-coefficient law $v_2(c_k)=4k$** ($k\ge1$), firmly established for
$k=1,2$. The $k=3$ confirmation ($v_2(c_3)=12$ expected) was the next computation when the Wolfram
MCP endpoint began returning intermittent **502 gateway errors** (api.anthropic.com, not a Wolfram
TLS issue); after several backoffs it remained unavailable, so $v_2(c_3)$ is recorded honestly as
$\ge11$ (observed at mod $2^{16}$), expected $12$ from the $4k$ law and the analyticity.

**(C4) The "$-4$" offset is the tangent valuation (Wolfram).** Since the leading slope $c_1$ has
$v_2(c_1)=4$, the affine part $A_0+c_1k_y$ already has period exactly $2^{n-4}$ at depth $n$ — this
**re-derives Theorem P's "$-4$" offset directly from the tangent valuation**, paralleling the
3-adic Session-24 result where $v_3(\tau)=3$ gives the 3-adic structure. The source on the 2-adic
side is $v_2(166320)=4$; on the 3-adic side it is $v_3(2^{332640}-1)=4$.

### D. New Wolfram subtlety — the genuine $z$ branch is selected archimedeanly

- $N=2^x+3^y+5\equiv1\pmod9$ **universally** over the $3\times3$ $(k_x,k_y)$ grid (Wolfram).
- $N$ has **three** 3-adic cube-root branches $\equiv1\pmod3$ (verified: exactly 3 cube roots of
  $N$ that are $\equiv1\pmod3$, mod $3^5$, in every grid cell). So congruence data at 3 do **not**
  single out $z$; the genuine $z$ is the one that is the *actual integer cube root* — an
  **archimedean** selection. This is a clean restatement of *why* the obstruction is archimedean:
  the $p$-adic data leave a bijective 3-adic branch ambiguity that only $A\approx2^{x/3}$ resolves.
- Since $j\ge55439\gg n$, $3^j\equiv0\pmod{3^n}$, hence $z\equiv A\pmod{3^n}$: **$z$ inherits
  Theorem J's 3-adic portrait exactly** (depends only on $k_x$), so the variable-separation
  Observation Q extends from $A$ to $z$.

### E. Survival consistency check (Wolfram)

Surviving pairs ($D=x\log2-(2j+1)\log3\ge0$) for small $k_x$ match Theorem M exactly
($k_y\le\lceil1.893\,k_x-1.107\rceil-1$), as in prior sessions.

---

## Results

1. **arXiv watch negative (twenty-second confirmation).** No new applicable paper; Bugeaud still
   "$\to\infty$ with $z^d$", $k\ge3$-prime decidability still open, degree-3 blocked, CDT/Badziahin
   $\mu(2^{1/3})>2$.

2. **Theorem R (NEW, Wolfram-verified): 2-adic Newton/tangent expansion of $k_y\mapsto A$.** In
   Mahler form $A(k_y)=c_0+c_1k_y+c_2\binom{k_y}{2}+\cdots$: $c_0\equiv11\pmod{16}$; $v_2(c_1)=4$,
   $v_2(c_2)=8$, leading law $v_2(c_k)=4k$ ($k=1,2$ confirmed at $2^{12},2^{16},2^{24}$). So
   $k_y\mapsto A$ is **2-adic analytic with tangent valuation 4**; the affine form is exact up to
   depth $n=8$, the $\binom{k_y}{2}$ term ($v_2=8$) first breaking it at $n=9$. The same expansion
   holds for $z$. This re-derives Theorem P's "$-4$" period offset from the slope and completes the
   2-adic/3-adic tangent-valuation symmetry (2-adic $v_2=4$ vs Session-24 3-adic $v_3=3$).

3. **New subtlety (Wolfram): the genuine $z$ branch is archimedean-selected.** $N\equiv1\pmod9$
   universally with **three** 3-adic cube-root branches $\equiv1\pmod3$; the integer $z$ is picked
   among them only by being the true cube root. And $z\equiv A\pmod{3^n}$, so $z$ shares $A$'s
   3-adic portrait and variable separation.

4. **Why this confirms (does not breach) the wall — honest.** Theorem R is a structural sharpening
   of the existing $p$-adic portrait. The 2-adic bijection's analyticity and tangent valuation 4
   are *internal* facts about the factorization; they impose no archimedean constraint. The
   three-branch subtlety makes explicit that the only thing tying the free 2-adic parameter ($k_y$)
   and free 3-adic parameter ($k_x$) to a genuine integer solution is the archimedean size
   $A\approx2^{x/3}$ — exactly the missing power-saving $\kappa>1$ gap bound. **Twenty-second
   structural confirmation**, now from the 2-adic-analyticity / branch-selection side.

---

## Conclusion

The research remains in pure WATCH phase; the conjecture is still reduced to the single
unconditional open target $|z^3-3^y|>C(z^3)^{\kappa}$ with fixed effective $\kappa>1$ — a tool that
does not exist. This session's substantive output is a **new, Wolfram-verified structural theorem**
(Theorem R, the 2-adic Newton/tangent expansion) that sharpens the Session-32 Theorem P from a
period statement to an explicit 2-adic analytic Mahler expansion with tangent valuation 4, and
completes the symmetry with the Session-24 3-adic tangent valuation 3. Together with the pinned
subtlety that the genuine cube-root branch is selected archimedeanly (the $p$-adic data leave a
bijective 3-adic branch ambiguity), it gives the sharpest available statement that *the only
non-$p$-adic information is the archimedean size* — i.e. the $\kappa>1$ gap. It is a sharpening, not
a breach.

No theory-building was performed: per the Session-28 inventory-complete verdict, a fourth framework
would collapse onto an existing attack face. Theorem R is a structural fact about the existing
factorization, recorded in `knowledge/problem.md`, not a new framework.

---

## State Changes

- Created `log/20260702_000000.md` (this file).
- Added **Theorem R** (2-adic Newton/tangent expansion of $k_y\mapsto A$) to `knowledge/problem.md`,
  after Observation Q.
- Updated `state/current_focus.md` to Session 33: twenty-second-confirmation watch; new Theorem R;
  WATCH remains the sole active priority; no framework built.
- Updated `state/directions.md`: added a Session-33 progress note to the factorization [HIGH]
  direction and a Session-33 History entry. No direction retired/elevated (no new attack face; the
  result confirms an existing face).
- No new dead end (this is a watch + confirming structural computation, not a
  newly-attempted-and-failed direction).

---

## Next Suggestions

1. **[WATCH — sole active priority]** Continue monitoring arXiv for:
   (a) a power-saving effective gap bound $|z^3-3^y|>C(z^3)^{\kappa}$ with fixed $\kappa>1$ in the
   regime $3^y\approx2^x\approx z^3$ — the necessary-and-sufficient tool, known to sit strictly
   above the realized exponent range $(2/3,1)$ (watch Bugeaud 2503.22084 / 2604.27490; CDT
   2510.04156; Badziahin 2301.02391);
   (b) a *special-structure* decidability/finiteness argument for the specific
   two-exponential-plus-single-cube Kuromine family, or the $k\ge3$-prime case (still "an
   outstanding open problem") — **NOT** a general degree-3 decidability theorem (arXiv:2510.00759
   proves none can exist);
   (c) effective equidistribution/discrepancy bounds beating the Dirichlet floor on the $2^{2/3}$
   approximation (equivalent to (a) by the S28 identity).

2. **[VERY LOW, optional cleanup]** When the Wolfram MCP endpoint is stable, confirm the auxiliary
   datum $v_2(c_3)=12$ (the $k=3$ Newton coefficient of $k_y\mapsto A$) at modulus $\ge2^{30}$, to
   fully nail the $v_2(c_k)=4k$ law for $k=3$. Purely a tidy-up of Theorem R; no new content.

3. **[META — inventory complete, re-affirmed]** All structurally distinct attack faces return the
   same barrier (twenty-two-plus confirmations). Do **not** re-derive a framework. The $p$-adic
   portrait of both factors is now complete down to the analytic tangent structure at both places
   (3-adic slope $v_3=3$, Session 24; 2-adic slope $v_2=4$, this session); the next genuinely new
   idea must beat the Dirichlet floor on the $2^{2/3}$ approximation or supply a power-saving
   $\kappa>1$ gap bound directly.
