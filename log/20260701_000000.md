# Session Log

**Session ID:** 20260701_000000 (Session 32)
**Date:** 2026-06-26 (scheduled run; next-timestamp convention 20260701_000000, after the latest
existing log 20260630_000000)
**Status:** WATCH-phase arXiv watch (**negative, twentieth confirmation**) followed by a concrete
Wolfram computation that produces a **genuinely new, verified structural result**: the **2-adic
mirror law for $A$** and the **variable-separation picture** of the $(2,3)$-adic portrait of $A$.
Like every prior structural finding, it **confirms (does not breach)** the two-comparable-S-units
wall — but it is a clean new theorem worth recording, not previously in the knowledge base. **No
theory-building** (per the Session-28 inventory-complete verdict, re-affirmed S29–S31).

*Wolfram MCP available; all numerical/symbolic claims below Wolfram-verified.*

---

## Direction Chosen

**[WATCH — the sole active priority]** plus a focused Wolfram experiment on the one piece of the
$p$-adic portrait that the knowledge base had recorded only as a *static* fact, never as a
*lifting law*: the **2-adic structure of $A = z - 3^j$**. The 3-adic side is fully developed
(Theorems I/J/K/L: $A \bmod 3^n$ determined by $k_x \bmod 3^{n-4}$, bijection $\mathbb Z_3 \to
\mathbb Z_3$). The 2-adic side appeared only as "$A \equiv 11 \pmod{16}$" (Theorem K) — its
lifting behaviour and dependence on the parameters had never been computed.

**Reasoning for the experiment (not theory-building).** The Session-28 verdict (re-affirmed
S29–S31) forbids a fourth framework: the attack-face inventory is complete and any new framework
collapses onto an existing face unless it beats the Dirichlet floor on the $2^{2/3}$
approximation. This session does **not** build a framework. It runs the mandated watch and then
computes a missing piece of the existing $p$-adic portrait — exactly the kind of concrete,
recordable structural sharpening that Sessions 29 (Eisenstein-$B$ density) and 31 ($E$-range)
produced. The honest expectation (borne out) was that it would confirm the wall while filling a
genuine gap in the documented structure.

---

## Work Done

### A. arXiv / web literature watch (negative; twentieth confirmation)

WebSearch over (1) effective power-saving lower bounds for $|z^d - S\text{-unit}|$ (2026,
Bugeaud); (2) decidability of linear-exponential Diophantine systems over $\ge 3$ primes /
Skolem (2026).

**All frontiers re-confirmed unchanged:**
- **Power-saving gap (Bugeaud).** arXiv:2503.22084 ("On the difference between squares and
  integral $S$-units") and the degree-$d$ generalization 2604.27490: effective lower bounds for
  $|z^d - q_1^{a_1}\cdots q_t^{a_t}|$ and its greatest prime factor that only **"tend to infinity
  with $z^d$"** — trivially met by the Kuromine gap $|z^3-3^y| = 2^x+5 \to\infty$; **NOT** a
  power-saving $\kappa>1$. Unchanged.
- **Decidability ($k\ge3$ primes).** Dong–Shafrir (arXiv:2505.19141, STOC 2026, "S-unit
  equations in modules and linear-exponential Diophantine equations"): solvability over $T$ with
  $\le 2$ prime divisors is decidable (via Karimov–Luca–Nieuwveld–Ouaknine–Worrell 2025); the
  $k\ge3$-prime case is still **"an outstanding open problem in number theory."** Degree-3 free
  terms remain blocked (S30 arXiv:2510.00759: degree 3 is the universal-undecidability
  threshold). No STOC/LICS 2026 follow-up reaches the Kuromine regime.

**Watch verdict: negative (twentieth confirmation of the two-comparable-S-units / degree-3 wall).**

### B. Wolfram re-anchor of frame constants (25 digits)

- $3 - 3\log_3 2 = 1.1072107392856276887014187\ldots$
- $3\log2/\log3 = 1.8927892607143723112985813\ldots$
- $2\log3/(3\log2) = 1.0566416671474374543024926\ldots$

### C. New Wolfram computation — the 2-adic portrait of $A$

Setup (Theorem-5 family): $x = 5 + 332640\,k_x$, $y = 166317 + 166320\,k_y$,
$j = y/3 = 55439 + 55440\,k_y$, $w = 3^j$, $N = 2^x + 3^y + 5 = z^3$, $A = z - w$.

**2-adic preliminaries (Wolfram).** $y$ is odd on the whole family ($166317$ odd, $166320$
even), so $3^y \equiv 3 \pmod 8$ and $v_2(3^y + 5) = 3$ **universally** (verified for
$k_y = 0..6$). Since $2^x$ has $v_2 = x \gg 3$, $v_2(N) = 3$, so $v_2(z) = 1$: $z \equiv 2
\pmod 4$, i.e. $z = 2u$ with $u$ odd and $u^3 = N/8$. (This re-derives the known "$z$ even"
fact and pins $v_2(z) = 1$ exactly.)

**The 2-adic cube root and $A \bmod 2^n$.** Since $2^x \equiv 0 \pmod{2^n}$ for $x \gg n$,
$N/8 \equiv (3^y+5)/8 \pmod{2^n}$, $u$ is its unique odd cube root mod $2^n$, and
$A = 2u - 3^j \pmod{2^n}$. Computed across the family:

| $n$ | modulus | $A \bmod 2^n$, $k_y = 0,1,2,3,4,5$ |
|----|---------|-----------------------------------|
| 4 | 16 | $11,11,11,11,11,11$ |
| 5 | 32 | $27,11,27,11,27,11$ |
| 6 | 64 | $59,43,27,11,59,43$ |
| 7 | 128 | $123,43,91,11,59,107$ |
| 8 | 256 | $251,43,91,139,187,235$ |
| 9 | 512 | $507,299,347,139,187,491$ |

**Theorem P (2-adic mirror law for $A$) — NEW, Wolfram-verified.**
1. $A \equiv 11 \pmod{16}$ **universally** (independent of $k_x, k_y$) — re-confirms Theorem K's
   $A \equiv 11 \pmod{16}$, now as the fixed 2-adic preamble (digits $[1,1,0,1]$).
2. For $n \ge 5$, $A \bmod 2^n$ is determined by **$k_y \bmod 2^{n-4}$** (and is independent of
   $k_x$). Verified: the number of distinct $A \bmod 2^n$ values over $k_y$ is exactly $2^{n-4}$
   ($n=5\to2$, $6\to4$, $7\to8$, $8\to16$, $9\to32$, $10\to64$, $11\to128$), and the period is
   **exactly** $2^{n-4}$ — the shift $k_y \mapsto k_y + 2^{n-4}$ preserves $A \bmod 2^n$, while
   the half-shift $k_y \mapsto k_y + 2^{n-5}$ does **not** (Wolfram, $n=6..10$).
3. Consequently the map $k_y \mapsto A$ is a **bijection $\mathbb Z_2 \to 11 + 16\,\mathbb Z_2$**;
   hence **no 2-adic obstruction exists** — the exact 2-adic analogue of Theorem J's 3-adic
   non-obstruction.

**Origin of the "$-4$" offset (Wolfram).** $\mathrm{ord}_{2^n}(3) = 2^{n-2}$ ($n\ge3$), and
$v_2(166320) = v_2(55440) = 4$; the period of $3^y \bmod 2^n$ in $k_y$ is therefore
$2^{n-2}/2^4 = 2^{n-6}$, lifted to $2^{n-4}$ for $A$ by the cube-root and $j$-shift. This mirrors
the 3-adic offset, which traces to $v_3(2^{332640}-1) = 4$ (Sessions 10/25). Both periods carry
the **same "$-4$"** — a numerical coincidence of the *specific* Theorem-5 moduli
($332640 = 2^5\,3^3\,5\,7\,11$, $166320 = 2^4\,3^3\,5\,7\,11$), Wolfram-confirmed.

### D. New Wolfram computation — the variable-separation picture

Computed $A \bmod 3^6 = 729$ and $A \bmod 2^7 = 128$ over a $3\times3$ grid of $(k_x, k_y)$:

- $A \bmod 729$ (rows $k_x = 0,1,2$; cols $k_y = 0,1,2$): $\{211,211,211\},\{22,22,22\},
  \{76,76,76\}$ — **depends only on $k_x$** (Theorem J).
- $A \bmod 128$ (rows $k_x = 0,1,2$; cols $k_y = 0,1,2$): $\{123,43,91\},\{123,43,91\},
  \{123,43,91\}$ — **depends only on $k_y$** (Theorem P).

**Observation Q (variable separation) — NEW.** The two exponential parameters control the two
places of $A$ **independently**: the **3-adic** digits of $A$ are a bijective function of $k_x$
alone, and the **2-adic** digits of $A$ are a bijective function of $k_y$ alone. The additive
constant $5$ couples the two places only through the single archimedean size relation
$A \approx 2^{x/3}$ (which involves *both* $k_x$ and $k_y$).

### E. Survival check (consistency)

Surviving pairs ($D = x\log2 - (2j+1)\log3 \ge 0$) for $k_x \le 4$: $(1,0)$; $(2,0),(2,1),(2,2)$;
$(3,0..4)$; $(4,0..6)$ — matching Theorem M exactly ($k_y \le \lceil 1.893\,k_x - 1.107\rceil - 1$).

---

## Results

1. **arXiv watch negative (twentieth confirmation).** No new applicable paper; Bugeaud still
   "$\to\infty$ with $z^d$", $k\ge3$-prime decidability still open, degree-3 blocked.

2. **Theorem P (NEW, Wolfram-verified): 2-adic mirror law for $A$.** $A \equiv 11 \pmod{16}$
   universally; for $n\ge5$, $A \bmod 2^n$ is determined by $k_y \bmod 2^{n-4}$ (period exactly
   $2^{n-4}$); $k_y \mapsto A$ is a bijection $\mathbb Z_2 \to 11+16\mathbb Z_2$. The $-4$ offset
   comes from $v_2(166320)=4$, mirroring the 3-adic $v_3(2^{332640}-1)=4$. This is the exact
   2-adic counterpart of the 3-adic Theorem J and was **not previously in the knowledge base**
   (which had only the static $A\equiv11\pmod{16}$).

3. **Observation Q (NEW): variable separation.** 3-adic digits of $A$ $\Leftrightarrow$ $k_x$
   (bijectively); 2-adic digits of $A$ $\Leftrightarrow$ $k_y$ (bijectively); the two places are
   independent, coupled only by the archimedean size relation $A\approx2^{x/3}$.

4. **Why this confirms (does not breach) the wall — honest.** Because each place is a *free*
   bijective parameter, **no** congruence/$p$-adic condition (at $2$, at $3$, or jointly via CRT)
   can pin $A$ off a genuine value: for any target integer $a \equiv 11 \pmod{16}$,
   $\equiv 22 \pmod{27}$, there exist $k_y \in \mathbb Z_2$ and $k_x \in \mathbb Z_3$ realizing
   $A = a$ in each place separately. The *only* place that ties $k_x$ and $k_y$ together is the
   archimedean size $A \approx 2^{x/3}$ — which is exactly the missing power-saving $\kappa>1$ gap
   bound. The variable-separation picture makes the obstruction maximally transparent: it is a
   **"2 integer parameters, but 3 independent matching conditions (2-adic, 3-adic, archimedean)"**
   over-determination heuristic — but the $p$-adic conditions are satisfiable in $\mathbb Z_2,
   \mathbb Z_3$ (not forced into $\mathbb Z$), so the heuristic is **not** a proof; it is the
   $p$-adic-side restatement of the same wall. **Twenty-first structural confirmation**, now from
   the explicit two-place bijection side.

---

## Conclusion

The research remains in pure WATCH phase; the conjecture is still reduced to the single
unconditional open target $|z^3 - 3^y| > C(z^3)^{\kappa}$, fixed effective $\kappa>1$ — a tool
that does not exist. This session's substantive output is a **new, clean, Wolfram-verified
structural theorem** (Theorem P, the 2-adic mirror law) plus its organizing **Observation Q**
(variable separation). These complete the $p$-adic portrait of $A$ — both places are now known to
be free bijective parameters, controlled by $k_y$ (the prime 2) and $k_x$ (the prime 3)
respectively — and thereby give the sharpest available explanation of *why* all congruence and
$p$-adic methods fail: $A$ is simultaneously a free 2-adic and a free 3-adic unit, tied only by
the archimedean size, which is precisely the $\kappa>1$ gap. It is a sharpening, not a breach.

No theory-building was performed: per the Session-28 inventory-complete verdict, a fourth
framework would collapse onto an existing attack face. Theorem P / Observation Q are *structural
facts about the existing factorization*, recorded in `knowledge/problem.md`, not a new framework.

---

## State Changes

- Created `log/20260701_000000.md` (this file).
- Added **Theorem P** (2-adic mirror law for $A$) and **Observation Q** (variable separation) to
  `knowledge/problem.md`, after Theorem N.
- Updated `state/current_focus.md` to Session 33: twentieth-confirmation watch; new Theorem P /
  Observation Q; WATCH remains the sole active priority; no framework built.
- Updated `state/directions.md`: added a Session-32 update to the factorization direction and a
  Session-32 History entry; added the twentieth arXiv-watch confirmation to the [LOW] survey
  direction. No direction retired/elevated (no new attack face; the result confirms an existing
  face).
- No new dead end (this is a watch + confirming structural computation, not a
  newly-attempted-and-failed direction).

---

## Next Suggestions

1. **[WATCH — sole active priority]** Continue monitoring arXiv for:
   (a) a power-saving effective gap bound $|z^3-3^y| > C(z^3)^{\kappa}$ with fixed $\kappa>1$ in
   the regime $3^y \approx 2^x \approx z^3$ — the necessary-and-sufficient tool, known to sit
   strictly above the realized exponent range $(2/3,1)$ (watch Bugeaud 2503.22084 / 2604.27490;
   CDT 2510.04156; Badziahin 2301.02391);
   (b) a *special-structure* decidability/finiteness argument for the specific
   two-exponential-plus-single-cube Kuromine family, or the $k\ge3$-prime case (still "an
   outstanding open problem") — **NOT** a general degree-3 decidability theorem (arXiv:2510.00759
   proves none can exist);
   (c) effective equidistribution/discrepancy bounds beating the Dirichlet floor on the $2^{2/3}$
   approximation (equivalent to (a) by the S28 identity).

2. **[META — inventory complete, re-affirmed]** All structurally distinct attack faces return the
   same barrier (twenty-plus confirmations). Do **not** re-derive a framework. Theorem P /
   Observation Q now give the cleanest *positive* statement of the obstruction's $p$-adic face:
   $A$ is a free 2-adic parameter (in $k_y$) and a free 3-adic parameter (in $k_x$), tied only by
   the archimedean size. The next genuinely new idea must beat the Dirichlet floor on the
   $2^{2/3}$ approximation or supply a power-saving $\kappa>1$ gap bound directly.

3. **[VERY LOW]** The variable-separation over-determination heuristic (2 integer parameters vs 3
   independent matching conditions) is suggestive but provably non-rigorous (the $p$-adic
   conditions are satisfiable in $\mathbb Z_2, \mathbb Z_3$, not forced into $\mathbb Z$). Logged
   for completeness; do not pursue as a proof route without an archimedean transfer tool.
